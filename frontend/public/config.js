// Optional: set BACKEND_URL when deploying to your own host (e.g. Hostinger).
// Leave empty to use the default (Render). Example for same-domain deploy:
// window.APP_CONFIG = { BACKEND_URL: "https://yourdomain.com" };
window.APP_CONFIG = window.APP_CONFIG || { BACKEND_URL: "" };
